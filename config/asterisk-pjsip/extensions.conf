[general]
static=yes
writeprotect=no
clearglobalvars=no

[default]
exten => _X.,1,Goto(internal,${EXTEN},1)

[internal]
exten => 100,1,Dial(PJSIP/100,25)
 same => n,Hangup()

exten => 200,1,Dial(PJSIP/200,25)
 same => n,Hangup()

exten => 600,1,Answer()
 same => n,Echo()
 same => n,Hangup()
